//instalaciones
1-clonar proyecto git
2-pip install virtualenv (instalar entorno virual) 
3-environment\Scripts\activate (activar entorno)
4- py -m pip install Django (instalar django)

//requerimientos
pip install python-decouple
pip install mysqlclient
python -m pip install Pillow


leer si falla mysqlclient 
https://blog.webmatrices.com/error-command-errored-out-with-exit-status-1-mysqlclient/
